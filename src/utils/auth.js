import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();

async function hashPassword(password) {
  // implement password hashing
}

async function generateToken(seller) {
  // implement token generation
}
